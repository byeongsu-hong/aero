{
  "name": "aero-contracts",
  "version": "0.1.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "truffle test",
    "test:gas": "GAS_REPORTER=true npm test",
    "console": "truffle console --network child",
    "compile": "truffle compile",
    "migrate:child": "truffle migrate --all --network child"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eth-gas-reporter": "^0.1.10",
    "openzeppelin-solidity": "^1.12.0",
    "truffle": "^4.1.14",
    "truffle-privatekey-provider": "^0.1.0"
  }
}
